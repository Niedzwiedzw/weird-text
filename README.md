# WeirdText encoder/decoder
Implementation of algorithm for WeirdText pseudoencryption of text. 
Requires `Python3.6+` ( due to usage of `f-strings` and `type hints`).

to run tests
```bash
python3.6 ./test.py
```